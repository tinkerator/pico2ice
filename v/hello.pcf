# This is an abbreviated version of the file in the sdk:
#
#   https://github.com/tinyvision-ai-inc/pico-ice-sdk/blob/main/rtl/pico2_ice.pcf
# We've only kept entries that our hello.v file uses.

set_io -nowarn clk    35
set_io -nowarn reset  11 # active-low
set_io -nowarn led_r  41 # active-low
set_io -nowarn led_g  39 # active-low
set_io -nowarn led_b  40 # active-low
